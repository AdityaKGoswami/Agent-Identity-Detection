# Copy this file to .env (same folder) and fill in your own key(s).
# .env is already in .gitignore -- it will never be committed.
# Only set the key for the backend you're actually using.

GROQ_API_KEY=
ANTHROPIC_API_KEY=

