pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
anthropic>=0.40    # only needed for agent_harness --backend real (paid)
openai>=1.0        # only needed for agent_harness --backend groq (free)
python-dotenv>=1.0 # loads GROQ_API_KEY / ANTHROPIC_API_KEY from a local .env file
